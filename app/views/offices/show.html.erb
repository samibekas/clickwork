<h1><%= @office.name %></h1>

<%= cl_image_tag @office.photo, width: 400, height: 300, crop: :fill %>

<h2>Chez <%= @office.user.email %></h2>
<h3> Description: <%= @office.description %></h3>
<p><%= @office.address %></p>

<p> Number of desks: <%= @office.capacity_max %></p>

<%= link_to "Back to list", offices_path %>

<%= simple_form_for [@office, @review] do |f| %>
  <%= f.input :comment %>
  <%= f.input :rating %>
  <%= f.submit "Add a review", class: "btn btn-primary" %>
<% end %>


<% if @reviews.nil? %>
  <p>No reviews Yet</p>
<% else %>
  <ul>
    <% @reviews.each do |review| %>
      <li>
        <%= review.created_at %>
        <%= review.comment %>
        <%= review.rating %>
        <%= review.user.first_name %> <%= review.user.last_name %>
      </li>
    <% end %>
  </ul>
<% end %>
